import { CustomDateFilterPipe } from './custom-date-filter.pipe';

describe('CustomDateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
