import { LocalTimeConverterPipe } from './local-time-converter.pipe';

describe('LocalTimeConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalTimeConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
