export interface validateObj {

    validateFlag : boolean;
    errorMsg ?: any;
    errorString ?: string

}


export interface uploadType {

    uploadFlag : boolean;
    errorMsg  ? : string;
    documentUploadId ? : string;
    type ? : string

}